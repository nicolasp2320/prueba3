<!DOCTYPE html>
<html>
<head>
  <title>Twitter</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= stylesheet_link_tag 'twitterstyles', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= stylesheet_link_tag 'twitterstyles', media: 'all', 'data-turbolinks-track': 'reload' %>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  
 
  <%= javascript_importmap_tags %>

  <style>
   
  </style>
</head>

<body style= "background-image: url('/wallpaper.jpg'); background-repeat: no-repeat; background-size: cover;">
  <div class="container ">
    <div class="row justify-content-center">
      <div class="col-8">
        <div class="d-flex justify-content-between mb-4 mt-4">
          <%= button_to "Nuevo tweet", new_tweet_path, method: :get, class: "btn btn-primary" %>
          <%= form_with url: tweets_path, method: :get, class: "form-inline" do |form| %>
            <div class="input-group">
              <%= form.text_field :query_text, placeholder: t('Query text'), value: params[:query_text], class: "form-control" %>
              <div class="input-group-append">
                <%= form.submit "Buscar", class: "btn btn-primary" %>
              </div>
            </div>
          <% end %>
        </div>

        <%= yield %>

        <div class="d-flex justify-content-center">
          <%= link_to "Inicio", tweets_path, class: "btn btn-primary" %>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
